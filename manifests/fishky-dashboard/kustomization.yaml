apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fishky

resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  # Secret NIE jest tutaj — zarządzany ręcznie (kubectl apply) poza ArgoCD,
  # zgodnie z konwencją z manifests/fishky/.
  # Patrz secret.yaml.example.
