apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mrozy-landing
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kompot-rar/kubernetes.git
    targetRevision: HEAD
    path: manifests/mrozy-landing
  destination:
    server: https://kubernetes.default.svc
    namespace: mrozy-landing
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
