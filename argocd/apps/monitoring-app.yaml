apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://prometheus-community.github.io/helm-charts
    targetRevision: 69.1.1
    chart: kube-prometheus-stack
    helm:
      values: |
        grafana:
          enabled: true
          ingress:
            enabled: true
            ingressClassName: traefik
            hosts:
              - grafana.k3s
        
        prometheus:
          prometheusSpec:
            retention: 10d
            serviceMonitorSelectorNilUsesHelmValues: false
            
            # MONITORING FIZYCZNY (VLAN 10) - OSZCZĘDNY
            additionalScrapeConfigs:
              - job_name: 'proxmox-nodes'
                scrape_interval: 180s # Co 3 minuty - starczy aż nadto
                static_configs:
                  - targets:
                    - '10.0.10.11:9100'
                    - '10.0.10.12:9100'
                    - '10.0.10.13:9100'
              - job_name: 'proxmox-smart'
                scrape_interval: 300s # SMART co 5 minut - idealny balans
                static_configs:
                  - targets:
                    - '10.0.10.11:9633'
                    - '10.0.10.12:9633'
                    - '10.0.10.13:9633'

            storageSpec:
              volumeClaimTemplate:
                spec:
                  accessModes: ["ReadWriteOnce"]
                  resources:
                    requests:
                      storage: 10Gi
            
        prometheusOperator:
          admissionWebhooks:
            enabled: false
            patch:
              enabled: false

        alertmanager:
          enabled: true
        
        nodeExporter:
          enabled: true
        
        kubeStateMetrics:
          enabled: true
  
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - Replace=true
