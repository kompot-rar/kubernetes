apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fixtime
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kompot-rar/kubernetes.git
    targetRevision: HEAD
    path: manifests/fixtime
  destination:
    server: https://kubernetes.default.svc
    namespace: fixtime
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
