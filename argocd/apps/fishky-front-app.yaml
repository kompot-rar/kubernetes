apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fishky-front
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kompot-rar/kubernetes.git
    targetRevision: HEAD
    path: manifests/fishky-front
  destination:
    server: https://kubernetes.default.svc
    namespace: fishky-front
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
